[project]
name = "mcp-server-xlsx"
version = "0.1.0"
description = "MCP server for reading and writing .xlsx files"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.0.0",
    "openpyxl>=3.1.0",
    "duckdb>=0.10.0",
]
